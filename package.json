{
  "private": true,
  "scripts": {
    "build": "node packages/react-scripts/bin/react-scripts build --debug",
    "changelog": "lerna-changelog",
    "create-react-app": "tasks/cra.sh",
    "e2e": "tasks/e2e-simple.sh",
    "e2e:docker": "tasks/local-test.sh",
    "postinstall": "node bootstrap.js && cd packages/react-error-overlay/ && npm run build:prod",
    "publish": "tasks/release.sh",
    "start": "node packages/react-scripts/bin/react-scripts start --debug",
    "test": "node packages/react-scripts/bin/react-scripts test --env=jsdom",
    "format": "prettier --trailing-comma es5 --single-quote --write 'packages/*/*.js' 'packages/*/!(node_modules)/**/*.js'",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "eslint": "3.19.0",
    "husky": "^0.13.2",
    "lerna": "2.0.0-rc.5",
    "lerna-changelog": "^0.2.3",
    "lint-staged": "^3.3.1",
    "prettier": "^1.5.2"
  },
  "lint-staged": {
    "*.js": [
      "prettier --trailing-comma es5 --single-quote --write",
      "git add"
    ]
  }
}
